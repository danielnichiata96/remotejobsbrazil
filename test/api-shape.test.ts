import { describe, it, expect } from "vitest";

describe("api shape (static check)", () => {
  it("dummy", () => {
    // This is a placeholder: full E2E would require server running.
    // We keep unit tests minimal for now.
    expect(true).toBe(true);
  });
});
