// Vitest stub for Next.js `server-only` module.
// In Next.js runtime, importing 'server-only' throws in the client to enforce RSC boundaries.
// For unit tests, we alias it to this empty module so imports succeed harmlessly.
export {};
